没有添加交易规则/市场规则 -- 这个可能涉及系统的改变

现在先只做一开始只有x那么多钱，后面可以做每月工资，并不复杂

整理各种test的使用方法，它们都测试了哪些功能

很多模块都没有经过严格的测试，甚至说由于是AI生成的，很多内部模块根本没有测试，只是说外部的某个接口是可以正常工作的

tasks里面的task file格式需要说明一下，tasks文件夹用于保存每次回测任务的输入，这样设计的好处是可以方便地复现某次回测任务

results里面的result file格式需要说明一下，results文件夹用于保存每次回测任务的输出，特别要注意把日志输出路径修改到results，每次运行的结果文件夹名也要在考虑一下，然后也要把所加载的tasks复制到results里面。